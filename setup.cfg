[metadata]
name = tradingwizard-ai-prompt
version = 0.1.0
description = Daily TradingWizard AI prompt generator for trending AI trading topics.

[options]
packages = find:
package_dir =
    = src
install_requires =
python_requires = >=3.9

[options.packages.find]
where = src
